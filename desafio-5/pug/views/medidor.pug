doctype html
html(lang='es')
    head
        meta(charset='UTF-8')
        meta(name='viewport', content='width=device-width', initial-scañe='1.0')
        title="medidor con pug"
    body
        h1!=title
        meter(min=min, max=max, value=nivel)
